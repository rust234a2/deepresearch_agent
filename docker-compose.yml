services:
  neo4j:
    image: neo4j:5
    container_name: deepresearch-neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: "${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-devpassword}"
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
